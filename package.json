{
  "name": "ai-lie-detection",
  "version": "1.0.0",
  "description": "A comprehensive project for AI-powered lie detection analysis.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/backend\" \"npm run dev --prefix apps/frontend\"",
    "build": "npm run build --prefix apps/backend && npm run build --prefix apps/frontend",
    "start:prod": "npm run start --prefix apps/backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
